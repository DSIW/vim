# VIM of DSIW

## Installation

    git clone https://github.com/DSIW/vim.git ~/.vim

## Requirements

* ctags for bundle TagBar
* ruby and ruby-support for bundle Command-T (maybe 1.8.7)
* patched font for bundling PowerLine in ~/.fonts
